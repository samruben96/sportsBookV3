import { EuropeToUSPipe } from './europe-to-us.pipe';

describe('EuropeToUSPipe', () => {
  it('create an instance', () => {
    const pipe = new EuropeToUSPipe();
    expect(pipe).toBeTruthy();
  });
});
